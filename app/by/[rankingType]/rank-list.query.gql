query Rankings($order: RankOrder, $offset: Int) {
  rankings(order: $order, offset: $offset) {
    githubId
    contributedStars
    contributedStarsM
    followersCount
    followersCountM
    ownedStars
    ownedStarsM
    user {
      login
      avatarUrl
      ownedStars
      contributedStars
      followersCount
      location
    }
  }
}
